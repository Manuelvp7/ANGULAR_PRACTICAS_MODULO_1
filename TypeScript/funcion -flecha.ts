(
    ()=>{

        const mayus = (a: string, b: string) => a.toUpperCase()+b.toUpperCase();
        const suma = (a:number,b:number) => a+b;

        console.log('MAYUS FUNCTION' + mayus('hola ','mundo'));

        console.log('SUMA '+ suma(10,5));




    }
)();